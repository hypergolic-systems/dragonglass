// Side-effect entry — Svelte 5's compiled-component output emits
// `import 'svelte/internal/disclose-version'` for runtime version
// signaling. Re-export for importmap visibility.
import 'svelte/internal/disclose-version';
